import { WorkingHoursChartDirective } from './working-hours-chart.directive';

describe('WorkingHoursChartDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkingHoursChartDirective();
    expect(directive).toBeTruthy();
  });
});
