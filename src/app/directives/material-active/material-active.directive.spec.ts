import { MaterialActiveDirective } from './material-active.directive';

describe('MaterialActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialActiveDirective();
    expect(directive).toBeTruthy();
  });
});
